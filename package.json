{
  "name": "atticusmini",
  "private": true,
  "description": "Root proxy so Render's default 'npm install && npm run build' resolves the AT_frontend Vite app.",
  "scripts": {
    "postinstall": "npm --prefix AT_frontend install --no-audit --no-fund",
    "build": "npm --prefix AT_frontend run build",
    "preview": "npm --prefix AT_frontend run preview",
    "typecheck": "npm --prefix AT_frontend run typecheck"
  }
}
